services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: calai
      POSTGRES_PASSWORD: calai
      POSTGRES_DB: calai
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  postgres_data:
